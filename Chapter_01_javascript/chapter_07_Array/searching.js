//searching

let arr =["pass", "fail", "pass", "error", "wrong"]
//indexOf -> returns 1st index, -1 if not found
arr.indexOf("fail") // 1
arr.indexOf(skip)  // -1

//lastIndexOf -> searches from the end
arr.lastIndexOf("wrong") // 4

//Includes -> returns boolean
arr.includes("error") // true
arr.includes("fail") // true
arr.includes("champaran") // false

//find -> retuens first matching element
let nums =[10, 20, 30, 55, 78]
nums.find(x => x>20) // 
console.log(nums)

//findIndex
nums.findIndex(n => n>20) 
